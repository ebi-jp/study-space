
.user__form
  .user__form__content
    .user__form__title 
    = link_to  'StudySpace','/', class: "header__title__signup--text"
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = render "devise/shared/error_messages", resource: resource
      .field
        = f.text_field :name, autofocus: true, placeholder: "ユーザーネーム",class:"field_box"
      .field
        .field_image
          .field_image_text 画像登録
          = f.label :image do
            %i.fa.fa-image.fa-2x
            = f.file_field :image, autofocus: true, autocomplete: "image",style: "display:none"
      .field
        = f.email_field :email, autofocus: true, placeholder: "メールアドレス",class:"field_box"
      .field
        - if @minimum_password_length
          %em
        = f.password_field :password, placeholder: "パスワード6文字以上",class:"field_box"
      .field
        = f.password_field :password_confirmation, placeholder: "確認パスワード",class:"field_box"
      .field
        = f.number_field :grade, autofocus: true, placeholder: "学年を入力",class:"field_box"
      
      .actions
        = f.submit "登録する",class:"field_submit"
    .user__form__login  アカウントお持ちですか？
    = link_to "ログイン", new_user_session_path,class: "link_login"
